# Uploads

This directory stores generated files such as PDF license certificates and other exports.

- `uploads/generated/` for generated application files
- `uploads/exports/` for manual or scheduled export bundles

`.gitkeep` files are included so the directory structure is preserved in Git.
